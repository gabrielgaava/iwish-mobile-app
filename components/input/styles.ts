import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderWidth: 1,
    borderColor: "#ffffff20",
    maxHeight: 60,
    minHeight: 60,
    marginTop: 10,
    marginBottom: 10,
    borderRadius: 10,
  },
  input: {
    flex: 1,
    width: "100%",
    outlineWidth: 0,
    paddingLeft: 10,
    paddingRight: 10,
    color: "#ffffff70",
  }
})